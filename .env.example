DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret
PAPERCLIP_TOOL_ACTION_SIGNING_SECRET=paperclip-dev-tool-action-signing-secret-change-me

# Discord webhook for daily merge digest (scripts/discord-daily-digest.sh)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# --- Trusted reverse-proxy authentication (see doc/REVERSE-PROXY-AUTH.md) ---
# Accept an authenticated identity from a forward-auth proxy header. The header
# IS the credential: only enable this when the listen port is unreachable
# except through the proxy. Requires PAPERCLIP_DEPLOYMENT_MODE=authenticated.
# PAPERCLIP_PROXY_AUTH_ENABLED=true
# PAPERCLIP_PROXY_AUTH_USER_HEADER=x-forwarded-user
# PAPERCLIP_PROXY_AUTH_AUTO_PROVISION=false
# PAPERCLIP_PROXY_AUTH_EMAIL_DOMAINS=example.com
