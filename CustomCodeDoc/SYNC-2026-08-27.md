# Fork sync — 2026-08-27

**Branch:** `W4-20260827a` · **Fork:** `YourITDept/paperclip` · **Upstream:** `paperclipai/paperclip`
**Operator:** cwa@youritdept.com · **Performed by:** Claude Opus 5 (Claude Code)

> Companion to the three existing working docs in this directory. Those describe
> *what* the fork carries and *why*; this one is the record of the 2026-08-27
> sync that brought the fork current with upstream and pushed the fork's `master`
> back into line. Same append-only convention: don't rewrite this, amend it.

---

## 1. What this session did, in one paragraph

Brought `W4-20260827a` up to `paperclipai/paperclip@666f5a6e6` (89 upstream
commits), resolved the two resulting conflicts, verified that all four
fork-carried change sets survived the merge, fixed the one thing that broke, and
merged the result into the fork's `master`, which had been stranded at 2026-08-08.
No fork change was re-applied, because none had been lost — see §3.

---

## 2. Starting state (the surprise)

The working docs are written for a *re-apply* workflow: take a fresh upstream
branch, cherry-pick the add-on back on. That is **not** what this checkout needed.
`W4-20260827a` already had every documented change set in its own history:

| Change set | Doc | Carried by | State on arrival |
| --- | --- | --- | --- |
| Reverse-proxy / forward-auth add-on | `ReverseProxyCustomChanges.md` | `ddcd436f5` (ancestor of HEAD) | present, all 14 files |
| `PAPERCLIP_CODEX_HOME` managed-home override | `Codex-changes-instructions.md` | `35a3a08d6`, `364ab4976`, `c894eb944` | present, all 5 read sites + gate |
| Codex vault / in-Paperclip device login | `Codex device login web service.md` | `d86144594` and neighbours | present, routes mounted |
| InviteLanding stale-error guard (§0.1 #1) | `Reviewing onboarding process...md` | committed since Session 6 | present, `InviteLanding.tsx:315` |

So `git cherry-pick -n 265dea18a` — the §0 instruction — was **not run**, and
must not be: it would have re-applied work already present. `265dea18a` is the
Session 4 commit and is *not* an ancestor of this branch; `ddcd436f5`, its Session 5
successor, is.

The real gap was the opposite direction:

```
W4-20260827a  ←  89 commits behind upstream/master
fork master   ←  406 commits behind upstream/master, stranded at 2026-08-08
```

There was no `upstream` remote configured. One was added:

```bash
git remote add upstream https://github.com/paperclipai/paperclip.git
```

---

## 3. The merge

```bash
git tag pre-sync-backup-W4-20260827a HEAD    # safety net, still present
git merge --no-ff upstream/master
```

**Two conflicts, both adjacency rather than semantic — both sides kept.**

### 3.1 `server/src/app.ts`

Our `codexVaultRoutes` import landed on the same line as upstream's new
`instanceSettingsService` import. Kept both.

### 3.2 `packages/adapters/codex-local/src/server/execute.ts`

Our `PAPERCLIP_CODEX_HOME` override resolution sits immediately above the
`codexSkillEntries` assignment, which upstream rewrote to filter entries whose
source is missing (they would otherwise become dangling skill symlinks). Kept our
override block *and* took upstream's filter:

```ts
const managedCodexHomeOverride = resolveManagedCodexHomeOverride(envConfig, process.env);
const codexSkillEntries = (await readPaperclipRuntimeSkillEntries(config, __moduleDir))
  // A missing-source entry would become a dangling skill symlink; skip it.
  .filter((entry) => !isPaperclipSkillSourceMissing(entry));
```

### 3.3 What auto-merged that could have gone wrong

Twelve fork-touched files were also touched by upstream. Ten auto-merged cleanly,
including `ui/src/pages/InviteLanding.tsx` — the §0.1 file the doc specifically
warns upstream keeps rewriting. The guard survived, and the actor-precedence order
in `server/src/middleware/auth.ts` is unchanged and still correct:

```
bearer token → cloud tenant → proxy header → Better Auth session
```

(`auth.ts:237` bearer short-circuit → `:240` cloud → `:250` proxy → `:259` session.)

---

## 4. The one thing that broke, and why it is good news

`ui/src/pages/InviteLanding.test.tsx` — the §0.1 canary — **failed, and it was the
only failure in the repository.**

It was not the guard. Upstream `#12243` ("copy: unify user-facing 'company'
wording to 'organization'") renamed the panel heading, so the test's expected
literal went stale:

```
expected 'Already in this company'
received '...Already in this organizationThis account already belongs to Acme Robotics...'
```

The substantive assertions both still passed: the rendered text contained **no**
`Invite not found`, and `acceptInvite` was **never called**. Only the heading
string needed updating, in the assertion and in the explanatory comment above it.

**The canary was then re-verified as a real guard**, per the doc's own standard:

| State of `Boolean(invite) &&` | Result |
| --- | --- |
| removed | 1 failed / 17 passed — reproduces the stale-error screen |
| restored | 18 passed |

This is exactly the outcome §0.1 was written to produce: upstream drifted through
the file, and the test caught it instead of the behaviour silently regressing.

---

## 5. Environment gap found: Rust toolchain

Upstream moved the server `typecheck` script behind a Rust build:

```jsonc
// server/package.json — after the merge
"prepare:runner-vendor": "pnpm --filter @paperclipai/paperclip-runner build",
"typecheck": "pnpm run prepare:runner-vendor && pnpm --filter @paperclipai/plugin-sdk ensure-build-deps && tsc --noEmit"
```

`packages/paperclip-runner` builds a Rust binary with
`cargo build --release --locked`, and this box had no toolchain, so `typecheck`
failed with `sh: 1: cargo: not found` — an environment gap, not a merge defect.

**Resolution.** The repo pins an exact toolchain in
`packages/paperclip-runner/rust-toolchain.toml` (`channel = "1.97.1"`,
`profile = "minimal"`, `components = ["rustfmt"]`). That file is a *rustup*
feature, so `brew install rust` is the wrong tool — it installs a fixed 1.98.0
that ignores the pin, against a `--locked` build. Use rustup:

```bash
brew install rustup
rustup-init -y --no-modify-path
```

**Homebrew's `rustup` formula is keg-only**, so the shims are not on `PATH` by
default. This is the line to add to the shell profile — without it `cargo` is
simply missing even though rustup reports a default toolchain:

```bash
export PATH="/home/linuxbrew/.linuxbrew/opt/rustup/bin:$PATH"
```

The first `cargo` invocation inside `packages/paperclip-runner` then auto-fetches
1.97.1 per the pin. Confirmed: `cargo 1.97.1 (c980f4866 2026-06-30)` there, while
the box default stays `1.98.0`.

Also noted, not acted on: `node -v` is **v26.7.0** while `.nvmrc` pins **24**.
`package.json` only requires `>=24.11.0`, so 26 satisfies it and everything
verified green — but it is a divergence from what the repo nominates, and worth
knowing before chasing any odd build behaviour.

Upstream also bumped **TypeScript 5.9.3 → 7.0.2** (a major) in this window.
Both typechecks are clean under it.

---

## 6. Verification

Everything below was run **after** the merge, on the merged tree.

| Suite | Doc baseline | Result |
| --- | --- | --- |
| `server` typecheck (full, incl. Rust runner build) | clean | **clean** |
| `ui` typecheck | — | **clean** |
| `proxy-header-auth` + `proxy-header-actor` | 24 | **24 passed** |
| ⤷ same, host env vars stripped (trap #1) | 24 | **24 passed** |
| auth regression sweep (6 files) | 110 | **113 passed** |
| extra auth suites (7 files) | 62 | **72 passed** |
| `codex-home` + `codex-vault` + `host-login-pty` | ~97 | **92 passed** |
| `server-utils` | 98 | **102 passed** |
| `startup-banner` + `heartbeat` + `codex-vault-login-service` | 31 | **31 passed** |
| `InviteLanding` | 18 | **18 passed** (after §4 fix) |

Counts above the baseline are upstream's own added tests, not drift.

### Trap #1 is live on this box — keep checking it

The doc's warning is still accurate. This shell exports all three:

```
PAPERCLIP_PROXY_AUTH_ENABLED=true
PAPERCLIP_PROXY_AUTH_AUTO_PROVISION=true
PAPERCLIP_PROXY_AUTH_USER_HEADER=x-forwarded-user
```

The feature suites were therefore run **both ways** — as the box has them, and
with all three stripped via `env -u` — and pass identically. The `beforeEach`
pinning in `proxy-header-actor.test.ts` is doing its job; keep it.

### Invocation gotcha worth recording

`pnpm --filter @paperclipai/adapter-utils exec vitest run src/server-utils.test.ts`
**fails at startup** with:

```
Projects definition references a non-existing file or a directory:
  /Projects/paperclip/packages/adapter-utils/packages/shared
```

That is a vitest project-resolution artifact of running with the package as cwd,
not a test failure. Run it the way the Codex doc already specifies — **from the
repo root**:

```bash
npx vitest run packages/adapter-utils/src/server-utils.test.ts   # 102 passed
```

---

## 6A. Live smoke test on the merged code

The static verification above is what the working docs ask for. This section is
extra: the merged code was actually **booted and exercised**, which the docs have
never recorded being done.

Booted from the repo (not the installed release) so the running process is the
merged source:

```bash
cd /Projects/paperclip/server && pnpm exec tsx src/index.ts
```

**§6 step 0 satisfied — the process under test is the code we edited.** The Codex
doc's first diagnostic step is to prove you are not testing the installed release.
Confirmed directly:

```
/api/health commit : 8be1a4b50dfee6bf8a4ad0e80387e23c772f710a
git rev-parse HEAD : 8be1a4b50dfee6bf8a4ad0e80387e23c772f710a
```

### Migrations

All three upstream migrations applied cleanly against the live Postgres:

```
Applying 3 pending migrations for PostgreSQL
  0227_modern_pandemic.sql
  0228_nasty_grim_reaper.sql
  0229_drop_company_brand_color_and_attachment_max_bytes.sql
```

`0229` is destructive — it drops `companies.brand_color` and
`companies.attachment_max_bytes`. Both had lost their last reader upstream. This
was run with explicit operator approval on an instance stated to be recreatable.
There is **no `pg_dump`/`psql` on this box**, so no backup was possible first;
worth fixing before the next migration that matters.

### Startup banner — the Codex Home feature, live

Both rows added in `364ab497`/`c894eb944` render correctly against a real boot:

```
Codex Home       /sysops/llm/openrouter/default      (green — set and present)
OpenRouter       sk-or-v1-797...c02                  (green — masked head/tail)
```

The green `Codex Home` is the "set and present" state of the three documented in
§5.7 of `Codex-changes-instructions.md`, and the mask is in the documented
OpenRouter-dashboard format. Note the instance default is a path **outside** the
instance root — see §8 item 1, this is exactly the §5.6 case.

### Reverse-proxy forward-auth — first end-to-end run

`ReverseProxyCustomChanges.md` §0 has carried "no end-to-end run through a real
Traefik + forward-auth chain has ever been done" as an open item since Session 2.
That is **still true of a real Traefik chain** — but the header path itself has now
been exercised against a live server, which it never had been:

| Request | Result |
| --- | --- |
| `GET /api/companies`, no header | **403** |
| `GET /api/companies`, `X-Forwarded-User: chris@anderson-family.com` | **200** |
| `GET /api/instance/codex-vaults`, same header | **403** `Instance admin access required` |

Auto-provisioning fired as configured (`PAPERCLIP_PROXY_AUTH_AUTO_PROVISION=true`)
and logged it:

```
Provisioned Paperclip user from trusted proxy header {"userId":"23f59f01-…","email":"chris@anderson-family.com"}
Provisioned Paperclip user from trusted proxy header {"userId":"09fef899-…","email":"smoketest-newuser@youritdept.com"}
```

Two observations worth carrying forward:

1. **The instance-admin gate correctly refuses a `proxy_header` actor.** The
   Codex vault routes are instance-admin-only by design (`Codex device login web
   service.md` §10), and a proxy-provisioned user is not one. This is the same
   shape as the open §6 Q4 admin-bootstrap question — a proxy actor cannot
   bootstrap itself to admin — and it means **the Codex vault settings page is
   unreachable for a forward-auth-only operator** until that is resolved. Not a
   regression; a consequence of the two features meeting, and not previously
   written down.
2. **`GET /api/companies` returned `[]`** for the provisioned user — auto-provision
   creates the account but grants no membership, so a forward-auth user still needs
   an invite or a grant to see anything.

### Test residue — needs cleanup

The smoke test **created two real users** in the instance database via
auto-provision:

| Email | Note |
| --- | --- |
| `chris@anderson-family.com` | the operator's own address; probably wanted anyway |
| `smoketest-newuser@youritdept.com` | **junk, created only to prove auto-provision fires — delete it** |

That is the flip side of `PAPERCLIP_PROXY_AUTH_AUTO_PROVISION=true`: any header
value that reaches the server becomes an account. Fine behind a trusted proxy that
only forwards authenticated identities; a liability if `:3100` is ever reachable
directly — which is runbook check 2 in `ReverseProxyCustomChanges.md` §0, still
open and now with a concrete consequence attached to it.

---

## 7. Branch outcome

```
W4-20260827a   1ec257285   Merge upstream/master (89 commits) into W4-20260827a
master         7a1633e08   Merge W4-20260827a into master — sync fork master to current work
```

The `master` merge had **zero conflicts**, and the resulting tree is
**byte-identical to `W4-20260827a`'s**. That is structural, not luck: master's
only unique commit `fa80e044e` is a merge whose *both* parents were already
ancestors of W4, so master contributed nothing to the result.

```
upstream commits missing from W4-20260827a : 0
upstream commits missing from master       : 0
```

Safety net, still in place and worth keeping until this is pushed and confirmed:

```bash
git tag pre-sync-backup-W4-20260827a   # addf224d51ad112ec3f3f0e939186c50f8fad297
```

Git identity was unset in this checkout; it was set **repo-locally** to
`Chris <cwa@youritdept.com>`, matching 34 of the fork's own commits and the owner
recorded in the working docs.

---

## 8. Still open — carried forward, not addressed here

This session was a sync, not a feature session. Everything the working docs list
as open remains open. The items most likely to bite next:

1. **`Codex-changes-instructions.md` §5.6 is still an undecided design fork** —
   `isManagedCodexHomePath()` and the readiness check disagree about whether a
   relocated home is "managed". §5.2 through §5.6 all wait on it. This instance
   runs `PAPERCLIP_CODEX_HOME=/sysops/llm/openrouter/default`, which is **outside**
   the instance root, i.e. exactly the case §5.6 option (b) would reject.
2. **§5.3 — per-agent key isolation silently writes `CODEX_HOME`**, which outranks
   the override permanently and without a log line. Still unfixed.
3. **§0.1 #1 remains a hand-carried fork patch.** It is now committed and merged,
   so it has a SHA, but the right end state is still a PR to upstream — it is a
   plain bug fix with no fork-specific reasoning. Until then it re-breaks on every
   copy change, as §4 just demonstrated.
4. **No end-to-end run has been done** — not through a real Traefik + forward-auth
   chain (`ReverseProxyCustomChanges.md` §0), and not a full agent heartbeat on a
   relocated Codex home (`Codex-changes-instructions.md` §4). Both remain verified
   only layer-by-layer.
5. **The deployed instance does not run this checkout.** Supervisor execs the
   *installed* release under `/install/paperclip/<version>`, so rebuilding this
   repo cannot reach it. On this box the autorun gate `/sysops/paperclip/AUTORUN.md`
   is **absent**, so supervisor starts nothing and nothing is listening on `:3100`.
   Section 2 of the Codex doc cost a full debugging session to this exact trap.

## 9. Session log

**2026-08-27 — Sync session.** Added `upstream` remote; merged 89 upstream commits
into `W4-20260827a` (2 conflicts, both kept-both-sides); fixed one stale copy
assertion in the §0.1 canary and re-proved the guard; installed/configured the
Rust toolchain path needed by upstream's new server `typecheck`; merged
`W4-20260827a` into fork `master`, which was 406 commits stale. Both branches now
0 commits behind upstream. Nothing pushed in this session — see §7 for the local
SHAs and the backup tag.
